BEGIN
  DECLARE
  seqval NUMBER;
  BEGIN
    SELECT NVL(MAX(VACANCY_DESIRED_HOUR_ID),0)  + 1
    INTO seqval
    FROM VACANCY_DESIRED_HOUR;
   
  
    execute immediate('CREATE SEQUENCE VACANCY_DESIRED_HOUR_SEQ MINVALUE 1 START WITH '||seqval||' INCREMENT BY 1 ORDER CACHE 20');
  END;
END;
/